<!DOCTYPE html>
<html>
<head>
</head>
<body>
  <h1> Links </h1>

    <ul id='links'>
      <% @links.each do |link| %>
      <li>
        Title: <%= link.title %>
        URL:   <%= link.url   %>
        Tags: <% link.tags.each do | tag | %>
               <%= tag == link.tags.last ? tag.name : (tag.name + ',') %>
             <% end %>
      </li>
      <% end %>
      <form action="/links/new" method="get">
        <input type="submit" value="Add new link">
    </ul>
</body>
</html>
